{
  "name": "777evaluator",
  "scripts": {
      "dev": "nuxt",
      "build": "nuxt build",      
      "generate": "nuxt generate",
      "build:gh-pages": "DEPLOY_ENV=GH_PAGES nuxt build",
      "generate:gh-pages": "DEPLOY_ENV=GH_PAGES nuxt build",
      "start": "nuxt start",
      "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "nuxt": "^2.14.6"
  },
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
